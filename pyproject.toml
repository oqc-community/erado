[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[project]
name = "erado"
version = "0.1.0"
description = ""
authors = [
    { name = "Sam Griffiths", email = "sgriffiths@oqc.tech" }
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
]


[tool.poetry]
packages = [{ include = "erado", from = "src" }]
